linters:
  disable:
    - cyclop
    - dupl
    - exhaustivestruct
    - forcetypeassert
    - funlen
    - gochecknoinits
    - gochecknoglobals
    - gocognit
    - gocyclo
    - godox
    - golint
    - gomnd
    - interfacer
    - maligned
    - nlreturn
    - paralleltest
    - scopelint
    - testpackage
    - wsl
  enable-all: true

linters-settings:
  forbidigo:
    forbid:
      - '^errors\.Wrap$'
      - '^errors\.Wrapf$'
      - '^fmt\.Errorf$'
  gci:
    local-prefixes: github.com/brandur

  gocritic:
    disabled-checks:
      - commentFormatting

  gosec:
    excludes:
      - G203

  wrapcheck:
    ignorePackageGlobs:
      - github.com/brandur/*
